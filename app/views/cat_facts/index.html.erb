<h1 class="text-3xl font-bold mb-4">Cat Fact 😺</h1>

<p class="mb-2 font-bold">Here is a cat fact for you:</p>
<p><span class="font-bold">#Fact <%= get_fact_id %></span> <%= @fact %></p>

<div class="flex text-white font-bold mt-4 text-center"> 
    <%= link_to "Get Another Fact 🐱", cat_facts_path, data: {turbo_method: :get}, class: "bg-blue-500 hover:bg-blue-700 py-2 px-4 rounded mr-4" %>
    <% if @added_to_favorites %>
        <%= link_to "Remove from Favorites 💔", favorite_path(get_fact_id), data: {turbo_method: :delete}, class: "bg-red-500 hover:bg-red-600 py-2 px-4 rounded mr-4" %>
    <% else %>
        <%= link_to "Add to Favorites 💖", favorites_path(cat_fact_id: get_fact_id), data: {turbo_method: :post}, class: "bg-red-500 hover:bg-red-600 py-2 px-4 rounded mr-4" %>
    <% end %>
    <%= link_to "Go to Favorites 🌟", favorites_path, class: "bg-green-500 hover:bg-green-600 py-2 px-4 rounded" %>
</div>
